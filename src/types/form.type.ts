export type TSearchBarOne = {
  query: string;
  genre: string;
  platform: string;
  year: string;
  sortBy?: string;
};

export type TFilterOptions = {
  genre: string;
  platform: string;
  year: string;
};
